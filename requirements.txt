# Theory-Constrained Supply Chain Resilience Agent
# Streamlit Cloud: use Python 3.11 in app Advanced settings if builds fail on 3.13.
# No crewai, scikit-survival, or mlflow here (they need Rust / build from source / pyarrow).

# Core ML & Survival
lifelines>=0.27.7
scikit-learn>=1.3.2
imbalanced-learn>=0.11.0

# Data & Graph
pandas>=2.0.3
networkx>=3.2.1
numpy>=1.24.3,<3

# Visualization & UI
plotly>=5.18.0
streamlit>=1.28.0

# mlflow omitted for Cloud (pyarrow build fails); app logs overrides only when mlflow is installed
